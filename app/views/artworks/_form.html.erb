<%= form_for(@artwork) do |f| %>
  <% if @artwork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork.errors.count, "error") %> prohibited this artwork from being saved:</h2>

      <ul>
      <% @artwork.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artwork_name %><br />
    <%= f.text_field :artwork_name %>
  </div>
  <div class="field">
    <%= f.label :artwork %><br />
    <%= f.text_field :artwork %>
  </div>
  <div class="field">
    <%= f.label :artwork_file_name %><br />
    <%= f.text_field :artwork_file_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
