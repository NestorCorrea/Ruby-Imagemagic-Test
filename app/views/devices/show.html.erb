<div class="main-div">
	<p id="notice"><%= notice %></p>

	<p><%= @debug %></p>
	<p>
		<%= link_to 'Back to devices', devices_path %>
	</p>

	<% @wrap_array.each do |device_image| %>
		<tr>
			<img src="/assets/image_exports/<%= device_image %>" height="400"/>
		</tr>
		<p>
			<%= device_image %>
		</p>

		<p>
			--------------------------------------------------------------------------
		</p>
	<% end %>
</div>

